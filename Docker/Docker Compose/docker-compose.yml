services:
  yararulegen:
    image: ghcr.io/05t3/yararulegen:${VERSION} # GHCR IMAGE
#    image: oste/yararulegen:1.0.0  # DOCKERHUB IMAGE
    container_name: yararulegen
    ports:
      - "${PORT}:5000" # DEFINE PORT in the .env file 
    volumes:
      - .:/app
    restart: unless-stopped